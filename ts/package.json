{
  "name": "oojs-ts",
  "version": "1.0.0",
  "description": "Object-Oriented JSON Schema validator — TypeScript implementation",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
